# Inspira Environment Configuration
# Copy this file to .env.local and modify as needed

# Backend Server URLs
# These will override the auto-detected host configuration
# Uncomment and modify for custom deployments

# Gateway Server (Node.js API server for AI generation)
# VITE_GATEWAY_SERVER_URL=http://your-server-ip:3003

# Development Notes:
# - Leave commented for local development (auto-detects localhost)
# - For network access, set your actual server IP address
# - Make sure the backend services are running on those ports
